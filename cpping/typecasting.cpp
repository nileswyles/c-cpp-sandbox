#include "stdio.h"

int main() {
    // So apparently you can't cast non-primitive types?
    //  primitives == int, double, pointers?, etc... 

    // Don't think I've ever done so?

    // 

    return 1;
}